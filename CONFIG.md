# CONFIG — Revisi Cepat
- **Warna**: edit token di `src/lib/colors.ts`. Nilai otomatis disuntik ke CSS variables.
- **Animasi**: preset Framer Motion di `src/lib/animations.ts` (durasi/easing/variants).
- **Harga**: helper dan diskon di `src/lib/pricing.ts`.

Ubah file-file ini, simpan, aplikasi akan memperbarui tema dan perilaku secara konsisten.
